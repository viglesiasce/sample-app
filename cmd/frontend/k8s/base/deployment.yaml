kind: Deployment
apiVersion: apps/v1
metadata:
  name: gceme-frontend
spec:
  selector:
    matchLabels:
      app: gceme
      role: frontend
      env: base
  template:
    metadata:
      name: frontend
      labels:
        app: gceme
        role: frontend
        env: base
    spec:
      containers:
      - name: frontend
        image: gcr.io/vic-cd-demo/sample-app-frontend
        args:
        - -backend-service=http://gceme-backend:8081
        env:
        - name: ENVIRONMENT
          value: base
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        imagePullPolicy: Always
        readinessProbe:
          initialDelaySeconds: 1
          periodSeconds: 1
          httpGet:
            path: /healthz
            port: 8080
        ports:
        - name: frontend
          containerPort: 8080
